# macOS Installer Build Requirements
# Based on working Windows installer dependencies

# Core build tools
pyinstaller>=6.0.0
setuptools>=65.0.0

# GUI framework options
# Option 1: tkinter (already in Python, same as Windows)
# Option 2: PyQt5 or PySide2 for native macOS look
# PyQt5>=5.15.0
# PySide2>=5.15.0

# HTTP client (same as Windows)
requests>=2.31.0

# JSON handling (built-in to Python)
# Standard library modules used:
# - os, subprocess, json, datetime, pathlib
# - shutil, tempfile, zipfile
# - threading, time

# Optional: Enhanced macOS integration
# pyobjc-framework-Cocoa>=9.0  # Native macOS APIs
# pyobjc-framework-Foundation>=9.0  # macOS Foundation framework

# Development and testing
pytest>=7.0.0  # For testing installer
black>=23.0.0   # Code formatting
flake8>=6.0.0   # Code linting

# ActivityWatch dependencies (if bundling)
# Note: ActivityWatch has its own dependencies
# aw-core>=0.5.0
# aw-client>=0.5.0
# aw-watcher-window>=0.4.0
# aw-watcher-afk>=0.4.0

# Build verification
psutil>=5.9.0   # System monitoring during testing